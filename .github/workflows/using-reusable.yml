name: Reusable Deploy
on: 
  workflow_call:
    inputs:
      artifacts-name:
        description: The name of the deploybale artifact files
        required: false
        default: dist
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Outut information name
        uses: actions/download-artificat@v3
        with:
          name: ${{ input.artifact-name }}
      - name: List files
        run: ls
      - name: Output information
        run: echo "Deploying & uploading..."
